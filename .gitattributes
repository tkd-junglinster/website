# Normalise line endings to LF in the repository for all text files.
# This keeps the source consistent across Windows/macOS/Linux and the
# Netlify (Linux) build.
* text=auto eol=lf

# Treat these as binary so Git never alters them.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary

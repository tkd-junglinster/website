---
import { getLangFromUrl, useTranslations, localePath } from '../i18n/utils';
import { content } from '../i18n/content';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const joinHref = localePath(lang, 'join');
const facts = [t('home.fact.1'), t('home.fact.2'), t('home.fact.3')];
const home = content[lang].home;
---

<!-- News banner -->
<div class="bg-brand-red text-white">
  <p class="mx-auto max-w-6xl px-4 py-3 text-center text-sm font-medium md:text-base">
    {home.newsBanner}
  </p>
</div>

<!-- Hero -->
<section class="relative isolate">
  <img
    src="/images/hero.png"
    alt=""
    class="absolute inset-0 h-full w-full object-cover"
    aria-hidden="true"
  />
  <div
    class="absolute inset-0 bg-gradient-to-r from-brand-black/90 via-brand-black/65 to-brand-black/30"
    aria-hidden="true"
  >
  </div>
  <div class="relative mx-auto max-w-6xl px-4 py-24 text-white md:py-36">
    <h1 class="max-w-2xl text-4xl font-extrabold leading-tight md:text-6xl">
      {t('home.hero.title')}
    </h1>
    <p class="mt-6 max-w-xl text-lg text-white/90 md:text-xl">
      {t('home.hero.subtitle')}
    </p>
    <a
      href={joinHref}
      class="mt-8 inline-block rounded-md bg-brand-red px-8 py-3 font-semibold text-white transition-colors hover:bg-brand-red-dark"
    >
      {t('home.cta')}
    </a>
  </div>
</section>

<!-- Welcome -->
<section class="mx-auto max-w-6xl px-4 py-16">
  <h2 class="border-l-4 border-brand-red pl-4 text-2xl font-bold md:text-3xl">
    {t('home.welcome.title')}
  </h2>
  <p class="mt-6 max-w-3xl text-lg text-brand-black/80">{t('home.welcome.body')}</p>
</section>

<!-- Free trial -->
<section class="bg-brand-black/[0.03]">
  <div class="mx-auto max-w-6xl px-4 py-14">
    <div class="flex flex-col items-start gap-4 rounded-lg border-l-4 border-brand-red bg-white p-6 shadow-sm md:flex-row md:items-center md:justify-between md:p-8">
      <div>
        <h2 class="text-2xl font-bold md:text-3xl">{home.trialTitle}</h2>
        <p class="mt-3 max-w-2xl text-brand-black/80">{home.trialBody}</p>
      </div>
      <a
        href={joinHref}
        class="shrink-0 rounded-md bg-brand-red px-8 py-3 font-semibold text-white transition-colors hover:bg-brand-red-dark"
      >
        {t('home.cta')}
      </a>
    </div>
  </div>
</section>

<!-- Key facts -->
<section class="bg-brand-black text-white">
  <div class="mx-auto grid max-w-6xl gap-8 px-4 py-16 md:grid-cols-3">
    {
      facts.map((fact) => (
        <div class="border-t-4 border-brand-red pt-4">
          <p class="text-lg font-medium">{fact}</p>
        </div>
      ))
    }
  </div>
</section>

<!-- Call to action band -->
<section class="bg-brand-red text-white">
  <div class="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 px-4 py-12 md:flex-row">
    <p class="text-xl font-semibold">{t('home.cta.band')}</p>
    <a
      href={joinHref}
      class="rounded-md bg-white px-8 py-3 font-semibold text-brand-red transition-colors hover:bg-white/90"
    >
      {t('home.cta')}
    </a>
  </div>
</section>
